{
    "runtime": "python2.7",
    "handler": "lambda.handle",
    "environment": {
    },
    "hooks": {
        "build": "pip install -r requirements.txt -t lib",
        "clean": "rm -rf lib"
    }
}