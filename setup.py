from distutils.core import setup

setup(
    name='arbor-one-core',
    version='0.1.1',
    author='Arbor Networks',
    packages=[
        'core',
        'functions.onefish'
    ],
    scripts=[
    ],
    install_requires=[
    ]
)